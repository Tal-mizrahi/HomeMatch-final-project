<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Define your color selector for BottomNavigationView items -->
    <!-- Color for selected/checked state -->
    <item android:color="@color/burnt_orange"  android:state_checked="true"/>
    <!-- Color for pressed state -->
    <item android:color="@color/burnt_orange" android:state_pressed="true"/>
    <!-- Default color -->
    <item android:color="@color/dark_gray"/>


</selector>